# Port of the dedicated Socket.IO service.
PORT=3002

# Comma-separated origins allowed to connect to the realtime server.
# Add the Vercel production URL before deployment.
CORS_ORIGINS=http://localhost:5173

# In-memory rooms are removed after this many inactive minutes.
ROOM_TTL_MINUTES=30
