# Local dedicated realtime server. Use the public HTTPS/WSS endpoint in Vercel.
VITE_REALTIME_URL=http://localhost:3002

# Optional. Defaults to the browser origin, so no production hostname is hard-coded.
# VITE_PUBLIC_APP_ORIGIN=http://localhost:5173
